
<h1 class="animate__animated animate__bounce"> Page profil de <%= current_user.first_name %> </h1>

Son prénom : <%= current_user.first_name %></br> 
son mail : <%=current_user.email %> </br>


<h3>Avatar actuel</h3>
<%if @user.avatar.attached?%>
  <%= image_tag @user.avatar, width: "15%", alt: 'avatar' %>
<%else%>
  <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
<%end%>

<h3>Changer d'avatar ?</h3>
<%= form_tag user_avatars_path(@user), multipart: true do %>
  <%= file_field_tag :avatar %>
  <%= submit_tag "mettre à jour" %>
<% end %>